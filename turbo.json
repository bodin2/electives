{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["GRADLE_USER_HOME"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".output/**", "build/**"],
      "outputLogs": "full"
    },
    "dev": {
      "cache": false,
      "env": ["APP_ENV=development"],
      "persistent": true,
      "outputLogs": "full"
    }
  }
}
